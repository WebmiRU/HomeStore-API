apiVersion: v1
kind: Service
metadata:
  name: www-service
  namespace: home-store
  labels:
    app: www
spec:
  selector:
    app: www
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: NodePort